@using AspNetMVCSandbox.Models

@model IEnumerable<CustOrderHist_Result>

@{
    Layout = null;
}
<style type="text/css">
    .box {
        width: 33%;
        height: 100px;
        display: inline-block;
        text-align: center;
        vertical-align: middle;
    }

    a:hover {
        outline: none;
        text-decoration: none;
    }

    .main {
        display: flex;
    }
    .color-red {
        color:red;
    }
    .color-green {
        color:green !important;
    }
</style>
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title></title>
</head>
<body>
    <div>
        <div class="container">
            @foreach (var item in Model)
            {
                @*<a href="#">
                        <div class='col-xs-12 col-sm-4 col-md-3 col-lg-3'>
                            <div class='panel panel-success'>
                                <div class='panel-heading'>@Html.DisplayFor(model => item.ProductName) - @Html.DisplayFor(model => item.Total)</div>
                            </div>
                        </div>
                    </a>*@
                <a class="success" href="#">
                    <div class="col-md-3 well" style="margin:2%;">
                        <div class="@(item.Total > 10 ? "color-green" : "color-red" )">
                            @Html.DisplayFor(model => item.ProductName)
                            -
                            @Html.DisplayFor(model => item.Total)

                        </div>
                    </div>
                </a>
            }
        </div>
        @*<div class="container">
                @{ var index = 0; }
                @foreach (var item in Model)
                {
                    if ((index + 3) % 3 == 0)
                    {
                        @:<div class="main">
                    }
                    <div class="box">
                        @Html.DisplayFor(model => item.ProductName)
                        <br />
                        @Html.DisplayFor(model => item.Total)
                    </div>
                    if ((index + 1) % 3 == 0)
                    {
                        @:</div>
                    }
                    index++;
                }
            </div>*@
        @*<table>
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.ProductName)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Total)
                        </th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.ProductName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Total)
                            </td>
                        </tr>
                    }
                </tbody>
            </table>*@
    </div>
</body>
</html>
